spring:
  datasource:
    username: postgres
    password: 12345678
    url: jdbc:postgresql://localhost:5434/scheduler-service-demo
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect